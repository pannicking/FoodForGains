<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <style>
        body {
            color: black;
            font-family: 'Times New Roman', serif;
            background-image: url("https://img.freepik.com/premium-photo/restaurant-cafe-coffee-shop-interior-with-people-abstract-defocused-blur-background_293060-3973.jpg");
            background-size: cover;
            background-attachment: fixed;
        }
    </style>
</head>

<body >
    <div class="container mt-4">
        <div class="text-center">
            <h2>Update Reservation</h2>
        </div>
        <br>
        <div class="text-center">
            <% if (reservation) { %>
                <form action="/editreservation/<%= reservation.reserve_id %>" method="POST">
                    <label for="name">Name:</label><br>
                    <input type="text" id="name" name="name" value="<%= reservation.name %>" required><br><br>

                    <label for="email">Email:</label><br>
                    <input type="email" id="email" name="email" value="<%= reservation.email %>" required><br><br>

                    <label for="contact">Contact:</label><br>
                    <input type="text" id="contact" name="contact" value="<%= reservation.contact %>" required><br><br>

                    <label for="pax">Number of Pax:</label><br>
                    <input type="number" id="pax" name="pax" min="1" step="1" value="<%= reservation.pax %>" required><br><br>

                    <label for="datetime">Date and Time:</label><br>
                    <input type="datetime-local" id="datetime" name="datetime"
                        value="<%= new Date(reservation.datetime).toISOString().slice(0, 16) %>" required><br><br>

                    <button type="submit" class="btn btn-primary">Update</button>
                </form>
            <% } else { %>
                <p>No reservation found.</p>
            <% } %>
            <a href="/" class="btn btn-secondary mt-3">Back</a>
        </div>
    </div>
</body>

</html>
